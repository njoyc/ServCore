services:
  - type: web
    name: servcore
    env: python
    plan: free
    branch: master
    runtime: python
    pythonVersion: 3.11.9
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn run:app
